<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.mybatis.mapper.oracle.files">
	<select id="selectfileUser" parameterType="int" resultType="filesVo">
		select product_no,product_name,files_no,files_name as file_name,original_file_name,files_size,user_no,order_date from userfilesview where user_no=#{userNo}
		order by file_name
	</select>
	<select id="selectfileExpert" parameterType="int" resultType="filesVo">
		select product_no,product_name,files_no,files_name as file_name,original_file_name,files_size,user_no,order_date from expertfilesview where user_no=#{userNo}
		order by file_name
	</select>
</mapper>



